# Handoff: Lean verifier scaffolding, mid-build-verification

**RESOLVED, 2026-07-15, on the upgraded Codespace: `lake build` finished
clean (`Build completed successfully`, both `#check`s resolved with the
expected types).** Confirmed the 2-core Codespace really was just
CPU-starving the build, not failing it — kept below for the record of
that diagnosis. See `Certification_Plan.md`'s PR 3 entry for current
status; this file no longer describes anything in flight.

Written 2026-07-15 because the Codespace was about to be upgraded to more
CPUs (the 2-core default was starving `lake build`) and the conversation
context might not survive that. Read this first, then
[`Certification_Plan.md`](Certification_Plan.md) for the full project plan
— this file is just "what's in flight right now and how to resume it."

## What's done and pushed

Branch `spanning-tree-cert`, commit `e832f33` (pushed to `origin`):

- **`lean-modulus` upstream change landed.** The two bridging lemmas
  (`Multigraph.isSpanningTree_iff_isBase`,
  `Multigraph.isBase_union_of_isBase_restrict_isBase_contract`) are on
  `lean-modulus`'s `main` branch, commit `1c91aa7412e0421dc8992c15f0932dda9f8756b2`.
  Confirmed directly against GitHub (not just trusting old planning-doc
  notes) — this resolved the push-blocked state described in
  `Certification_Plan.md` §4 Phase B.
- **New `lean/` directory scaffolded** (this repo, PR 3 in the plan's
  terms — but don't use that language in anything committed, per earlier
  instruction):
  - `lean/lakefile.toml` — package `discrete_modulus_cert`, library
    `DiscreteModulusCert`, `lean-modulus` pinned as a git dependency at
    the exact commit above (not `rev = "main"` — deliberate, for TCB
    auditability per the plan's §3).
  - `lean/lean-toolchain` — `leanprover/lean4:v4.32.0-rc1`, matching
    `lean-modulus`'s own toolchain.
  - `lean/DiscreteModulusCert.lean` + `lean/DiscreteModulusCert/Basic.lean`
    — the import smoke test. Imports `LeanModulus.Common.GraphicMatroid`
    and Mathlib's `Matroid.Minor.{Contract,Restrict}`, then `#check`s both
    lemmas by full name. Deliberately does **not** re-prove them locally
    (the earlier throwaway spike in `scratch/lean_spike/` did that, before
    the lemmas were upstreamed — now that they live in `lean-modulus`
    itself, redeclaring them here would just shadow/duplicate).
  - `lean/lake-manifest.json` — generated by `lake update`, committed as
    the lockfile (standard Lake practice, matches `scratch/lean_spike/`'s
    own committed manifest).
  - `lean/setup.sh` — one-time local bootstrap (`elan` install if missing,
    `lake update`, `lake exe cache get`, `lake build`). Deliberately not
    run automatically by the main devcontainer (Mathlib's cache is
    several GB) — see the plan's PR 3 checklist for why.
  - `.github/workflows/lean-test.yml` — CI job mirroring `cpp-test.yml`'s
    shape (installs `elan`, `lake update`, `lake exe cache get`,
    `lake build`), scoped to changes under `lean/`.
  - `README.md` — updated with a `lean/` entry in the repo layout, a
    "Building the Lean verifier" section, a CI table row, and a
    devcontainer note.

## What's NOT done yet — pick up here

- **`lake build` was still running when the Codespace got resized.** It
  had gotten *past* the slow part — `lean-modulus`'s own
  `Multigraph.lean` and `GraphicMatroid.lean` (the file with the two
  target lemmas) had both finished — and was on the very last step,
  compiling our own two-line `DiscreteModulusCert/Basic.lean` smoke test,
  when this doc was written. It had not yet been confirmed to actually
  succeed (no `lake build` exit code observed).
- **First thing to do in the new Codespace:**
  ```sh
  cd lean
  lake build
  ```
  This should be fast now — `lake update` already ran here and
  `lake-manifest.json` is committed, so it'll resolve deps from the lock
  file; Mathlib's cache will need re-fetching though (`lake exe cache get`
  first if `lake build` complains about missing `.olean`s — the cache
  itself lives under `lean/.lake/`, which is gitignored, so it does *not*
  travel with the pushed commit). If `lake build` reports success with
  the two `#check` lines resolving (no `sorry`/unknown-identifier errors),
  the smoke test has passed and PR 3's core goal is done.
- **Known environment gotcha, if it recurs on the new machine:** this
  Codespace had **0 swap** and only **2 CPUs**. The build wasn't a memory
  problem (available RAM held steady around 3.1–3.4GB the whole time,
  confirmed via a monitored `free -m` loop) — it was CPU starvation
  (`lean`'s process showed ~2.7% average CPU against a load average of
  5.3–5.8 on 2 cores). More CPUs should directly fix the wall-clock time;
  no need to re-diagnose this if the same slowness shows up again on a
  bigger machine, just check `nproc`/`uptime` load again to confirm it's
  actually resolved.
- **After `lake build` is confirmed green**, remaining PR 3 checklist
  items per `Certification_Plan.md` §4 (Phase B, PR 3) are already
  satisfied by what's pushed — there isn't a further "finish PR 3" step
  beyond confirming the build. The plan doc's own PR 3 checkboxes haven't
  been ticked yet in the committed version of `Certification_Plan.md` —
  worth updating those (`[ ]` → `[x]`) once the build is confirmed, plus
  noting the upstream `lean-modulus` merge landed.
- **Next real milestone after that is PR 4** (§4): defining spanning
  trees as a `FamilyOfObjects`, `𝒫(Γ)`/`𝒩ᵀμ`, and proving the
  certificate-optimality lemma — see the plan doc for the full checklist.
  Nothing about PR 4 has been started.

## Quick sanity check on resume

```sh
git log --oneline -3          # should show e832f33 at or near HEAD
git status                    # should be clean (or only .lake/ untracked, which is gitignored)
cd lean && lake build         # the actual thing to finish
```
