{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"discrete_modulus","text":"<p>API reference for the <code>discrete_modulus</code> Python package \u2014 reference implementations of algorithms for discrete modulus. For the underlying theory, see the companion book.</p> <p>Browse the API reference \u2192</p>"},{"location":"reference/","title":"API Reference","text":"<ul> <li>Algorithms \u2014 core solvers for discrete modulus: a   direct LP solver (<code>matrix_modulus</code>) and the incremental \"basic   algorithm\" (<code>modulus</code>).</li> <li>Families \u2014 functor classes/operators implementing families of objects   (<code>ShortestObjectFinder</code>s) for use with <code>algorithms.modulus</code>:   NetworkX families,   family operators.</li> <li>Demo \u2014 small graphs and brute-force helpers for   demonstrations.</li> <li>Protocols \u2014 shared result types and structural   interfaces.</li> </ul>"},{"location":"reference/algorithms/","title":"Algorithms","text":""},{"location":"reference/algorithms/#discrete_modulus.algorithms","title":"discrete_modulus.algorithms","text":"<p>Core solvers for discrete modulus: a direct LP solver (<code>matrix_modulus</code>) and the incremental \"basic algorithm\" (<code>modulus</code>) that grows the constraint set one object at a time via a <code>ShortestObjectFinder</code>.</p>"},{"location":"reference/algorithms/#discrete_modulus.algorithms.matrix_modulus","title":"matrix_modulus","text":"<pre><code>matrix_modulus(\n    N: FloatArray | spmatrix,\n    p: float = 2.0,\n    sigma: FloatArray | None = None,\n) -&gt; SubproblemResult\n</code></pre> <p>Computes the modulus of a family given its N matrix.</p> <p>Parameters:</p> Name Type Description Default <code>N</code> <code>numpy array OR scipy sparse matrix</code> <p>The N matrix for the family.</p> required <code>p</code> <code>float or inf</code> <p>The modulus energy exponent.</p> <code>2.0</code> <code>sigma</code> <code>numpy array</code> <p>The weights sigma.  If sigma = None, all weights are treated as 1.</p> <code>None</code> <p>Returns:</p> Type Description <code>SubproblemResult</code> <p>A named tuple <code>(mod, rho, lam)</code>:</p> <p>mod : float     An approximation of the modulus.</p> <p>rho : numpy array     An optimal rho^* for modulus.</p> <p>lam : numpy array     An optimal dual lambda^* for modulus.</p> Warnings <p>Emits a UserWarning if the underlying cvxpy solve does not report status \"optimal\" (e.g. \"optimal_inaccurate\" or \"infeasible\"). <code>rho</code>/<code>lam</code> are still returned in this case, but may not be reliable.</p> Notes <p>This function is just a template solver.  For specialized problems, it is probably best to implement a particular solver based on the energy exponent and the desired tolerances in the approximation.</p> Source code in <code>discrete_modulus/algorithms.py</code> <pre><code>def matrix_modulus(\n    N: FloatArray | sp.spmatrix,\n    p: float = 2.0,\n    sigma: FloatArray | None = None,\n) -&gt; SubproblemResult:\n    \"\"\"\n    Computes the modulus of a family given its N matrix.\n\n    Parameters\n    ----------\n    N : numpy array OR scipy sparse matrix\n        The N matrix for the family.\n\n    p : float or np.inf\n        The modulus energy exponent.\n\n    sigma : numpy array\n        The weights sigma.  If sigma = None, all weights\n        are treated as 1.\n\n    Returns\n    -------\n    SubproblemResult\n        A named tuple `(mod, rho, lam)`:\n\n        mod : float\n            An approximation of the modulus.\n\n        rho : numpy array\n            An optimal rho^* for modulus.\n\n        lam : numpy array\n            An optimal dual lambda^* for modulus.\n\n    Warnings\n    --------\n    Emits a UserWarning if the underlying cvxpy solve does not report\n    status \"optimal\" (e.g. \"optimal_inaccurate\" or \"infeasible\").\n    `rho`/`lam` are still returned in this case, but may not be reliable.\n\n    Notes\n    -----\n    This function is just a template solver.  For specialized\n    problems, it is probably best to implement a particular\n    solver based on the energy exponent and the desired\n    tolerances in the approximation.\n    \"\"\"\n\n    # problem dimension\n    m = N.shape[1]\n\n    # set sigma to default if necessary\n    if sigma is None:\n        sigma = np.ones(m)\n\n    # convert inputs to cvxpy constants\n    N_const = cvx.Constant(N)\n    sigma_const = cvx.Constant(sigma)\n\n    # primal variables\n    rho = cvx.Variable(m)\n\n    # objective\n    if p != np.inf:\n        obj = cvx.Minimize(sigma_const.T @ rho**p)\n    else:\n        obj = cvx.Minimize(cvx.max(cvx.multiply(sigma_const, rho)))\n\n    # constraints\n    cons = [rho &gt;= 0, N_const @ rho &gt;= 1]\n\n    # set up the problem\n    prob = cvx.Problem(obj, cons)\n\n    # attempt to solve\n    prob.solve()\n    if prob.status != \"optimal\":\n        warn(f\"cvxpy solve returned status {prob.status}\", stacklevel=2)\n\n    # rho and lam are constrained to be &gt;= 0 by the LP itself; clip away\n    # any tiny negative solver noise so downstream consumers (e.g.\n    # Dijkstra-based shortest object finders) never see a spurious\n    # negative weight.\n    rho_opt = np.array(rho.value).flatten().clip(min=0)\n    lam_opt = np.array(cons[1].dual_value).flatten().clip(min=0)\n\n    return SubproblemResult(prob.value, rho_opt, lam_opt)\n</code></pre>"},{"location":"reference/algorithms/#discrete_modulus.algorithms.modulus","title":"modulus","text":"<pre><code>modulus(\n    m: int,\n    solve_subproblem: SubproblemSolver,\n    find_shortest: ShortestObjectFinder,\n    p: float = 2.0,\n    sigma: FloatArray | None = None,\n    tol: float = 0.001,\n    max_iter: int = 1000,\n    output_every: int | None = None,\n) -&gt; ModulusResult\n</code></pre> <p>Implements the basic algorithm for modulus.</p> <p>Parameters:</p> Name Type Description Default <code>m</code> <code>int</code> <p>The dimension of the modulus problem (number of edges).</p> required <code>solve_subproblem</code> <code>SubproblemSolver</code> <p>See below.</p> required <code>find_shortest</code> <code>ShortestObjectFinder</code> <p>See below.</p> required <code>p</code> <code>float or inf</code> <p>The modulus energy exponent.</p> <code>2.0</code> <code>sigma</code> <code>numpy array</code> <p>The weights sigma.  If sigma = None, all weights are treated as 1.</p> <code>None</code> <code>tol</code> <code>float</code> <p>The tolerance.  The modulus algorithm stops when the approximate density is within tol of being admissible.</p> <code>0.001</code> <code>max_iter</code> <code>int</code> <p>Maximum number of iterations to perform before terminating with an error.</p> <code>1000</code> <code>output_every</code> <code>int</code> <p>Frequency of output to stderr.  If this is set to None, output is supressed.</p> <code>None</code> <p>Returns:</p> Type Description <code>ModulusResult</code> <p>A named tuple <code>(mod, cons, rho, lam)</code>:</p> <p>mod : float     Approximation to modulus.</p> <p>cons : list     List of constraints added during the iteration.  The format of the elements of     this list is determined by the <code>cons</code> field returned by <code>find_shortest</code>.</p> <p>rho : numpy array     Approximation to an optimal density.</p> <p>lam : numpy array     Approximation to the dual variables for the constraints listed in cons.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If the algorithm fails to converge to within <code>tol</code> after <code>max_iter</code> iterations.</p> Notes <p><code>solve_subproblem</code> and <code>find_shortest</code> are structural interfaces (see <code>discrete_modulus.protocols</code>), not concrete classes to inherit from.</p> <p><code>solve_subproblem</code> should have the signature</p> <pre><code>result = solve_subproblem(N, p, sigma)\n</code></pre> <p>returning a <code>SubproblemResult</code>. See <code>matrix_modulus</code> for an example.</p> <p><code>find_shortest</code> should have the signature</p> <pre><code>result = find_shortest(rho, tol)\n</code></pre> <p>returning a <code>ShortestResult</code>. This function should find a \"most violated constraint\" using the specified values for rho. Upon return, <code>result.cons</code> may contain any representation desired for describing the constraint.  (This is purely informational for the user and it is acceptable for <code>result.cons</code> to be set to None.)  <code>result.n</code> should contain the numpy row vector representing the violated constraint. This is the row that will be added to N on the next iteration.</p> <p>The argument tol may be ignored.  However, it is acceptable for the function to return <code>ShortestResult(None, None)</code> if every constraint is satisfied to within a tolerance of tol.</p> Source code in <code>discrete_modulus/algorithms.py</code> <pre><code>def modulus(\n    m: int,\n    solve_subproblem: SubproblemSolver,\n    find_shortest: ShortestObjectFinder,\n    p: float = 2.0,\n    sigma: FloatArray | None = None,\n    tol: float = 1e-3,\n    max_iter: int = 1000,\n    output_every: int | None = None,\n) -&gt; ModulusResult:\n    \"\"\"\n    Implements the basic algorithm for modulus.\n\n    Parameters\n    ----------\n    m : int\n        The dimension of the modulus problem (number of edges).\n\n    solve_subproblem : SubproblemSolver\n        See below.\n\n    find_shortest : ShortestObjectFinder\n        See below.\n\n    p : float or np.inf\n        The modulus energy exponent.\n\n    sigma : numpy array\n        The weights sigma.  If sigma = None, all weights\n        are treated as 1.\n\n    tol : float\n        The tolerance.  The modulus algorithm stops when the approximate\n        density is within tol of being admissible.\n\n    max_iter : int\n        Maximum number of iterations to perform before terminating with an error.\n\n    output_every : int\n        Frequency of output to stderr.  If this is set to None, output is supressed.\n\n    Returns\n    -------\n    ModulusResult\n        A named tuple `(mod, cons, rho, lam)`:\n\n        mod : float\n            Approximation to modulus.\n\n        cons : list\n            List of constraints added during the iteration.  The format of the elements of\n            this list is determined by the `cons` field returned by `find_shortest`.\n\n        rho : numpy array\n            Approximation to an optimal density.\n\n        lam : numpy array\n            Approximation to the dual variables for the constraints listed in cons.\n\n    Raises\n    ------\n    RuntimeError\n        If the algorithm fails to converge to within `tol` after `max_iter`\n        iterations.\n\n    Notes\n    -----\n    `solve_subproblem` and `find_shortest` are structural interfaces (see\n    `discrete_modulus.protocols`), not concrete classes to inherit from.\n\n    `solve_subproblem` should have the signature\n\n        result = solve_subproblem(N, p, sigma)\n\n    returning a `SubproblemResult`. See `matrix_modulus` for an example.\n\n    `find_shortest` should have the signature\n\n        result = find_shortest(rho, tol)\n\n    returning a `ShortestResult`. This function should find a \"most\n    violated constraint\" using the specified values for rho. Upon return,\n    `result.cons` may contain any representation desired for describing\n    the constraint.  (This is purely informational for the user and it is\n    acceptable for `result.cons` to be set to None.)  `result.n` should\n    contain the numpy row vector representing the violated constraint.\n    This is the row that will be added to N on the next iteration.\n\n    The argument tol may be ignored.  However, it is acceptable for the\n    function to return `ShortestResult(None, None)` if every constraint is\n    satisfied to within a tolerance of tol.\n    \"\"\"\n\n    # timers\n    search_time = 0.0\n    update_time = 0.0\n    mod_start = perf_counter()\n\n    # initialize variables\n    rho = np.zeros(m)\n    N = sp.csr_matrix((0, m))\n    lam = np.array([])\n    mod = 0.0\n    upper: float = np.inf\n    cons: list[object] = []\n\n    # default sigma\n    if sigma is None:\n        sigma = np.ones(m)\n\n    # initialize output table\n    if output_every:\n        print(\n            \"| {:&gt;6s} | {:&gt;9s} | {:&gt;9s} | {:&gt;9s} | {:&gt;6s} | {:&gt;9s} |\".format(\n                \"it\", \"l bnd\", \"u bnd\", \"rel gap\", \"# cons\", \"time (s)\"\n            )\n        )\n        print(\"+--------+-----------+-----------+-----------+--------+-----------+\")\n\n    # loop to at most max_iter\n    for iter_count in range(max_iter):\n        # find a constraint to add\n        start = perf_counter()\n        c, n = find_shortest(rho, tol)\n        search_time += perf_counter() - start\n\n        # compute the length of the shortest object\n        if n is None:\n            length = 1.0\n        else:\n            length = n.dot(rho)\n\n        # update the upper bound\n        if length &gt; 0:\n            if p == np.inf:\n                upper = np.max(np.abs(sigma * rho / length))\n            else:\n                upper = np.sum(sigma * (rho / length) ** p)\n\n        # check if we can stop\n        if length &gt; 1 - tol:\n            if output_every:\n                rel_gap = (upper - mod) / mod\n                elapsed = perf_counter() - mod_start\n                print(\n                    f\"| {iter_count + 1:6d} | {mod:9.3e} | {upper:9.3e} | {rel_gap:9.3e} \"\n                    f\"| {N.shape[0]:6d} | {elapsed:9.3e} |\"\n                )\n\n                print()\n                print(f\"program running time = {perf_counter() - mod_start} sec\")\n                print(f\"constraint search    = {search_time} sec\")\n                print(f\"solution update      = {update_time} sec\")\n\n            return ModulusResult(mod, cons, rho, lam)\n\n        # if not, we need to add a constraint\n        assert n is not None\n        # scipy-stubs doesn't model vstack's runtime support for mixing\n        # sparse and dense blocks; this call is valid at runtime.\n        N = sp.vstack([N, n], format=\"csr\")  # type: ignore[call-overload]\n        cons.append(c)\n\n        # re-optimize\n        start = perf_counter()\n        mod, rho, lam = solve_subproblem(N, p, sigma)\n        update_time += perf_counter() - start\n\n        # print some feedback if desired\n        if output_every and (iter_count + 1) % output_every == 0:\n            if mod == 0:\n                rel_gap = np.inf\n            else:\n                rel_gap = (upper - mod) / mod\n            elapsed = perf_counter() - mod_start\n            print(\n                f\"| {iter_count + 1:6d} | {mod:.3e} | {upper:.3e} | {rel_gap:.3e} \"\n                f\"| {N.shape[0]:6d} | {elapsed:.3e} |\"\n            )\n\n    # if we got here, we failed to converge\n    raise RuntimeError(f\"Modulus algorithm failed to converge in {max_iter} iterations.\")\n</code></pre>"},{"location":"reference/demo/","title":"Demo","text":""},{"location":"reference/demo/#discrete_modulus.demo","title":"discrete_modulus.demo","text":"<p>Small graphs and brute-force helpers for demonstrations.</p> <p>These are meant for small pedagogical examples (as in the companion book), not for production use \u2014 see <code>spanning_trees</code> in particular.</p>"},{"location":"reference/demo/#discrete_modulus.demo.house_graph","title":"house_graph","text":"<pre><code>house_graph() -&gt; tuple[nx.Graph, PosDict]\n</code></pre> <p>Generates the house graph.</p> <p>Returns:</p> Name Type Description <code>G</code> <code>networkx graph</code> <p>The house graph.</p> <code>pos</code> <code>dict</code> <p>Position dictionary for drawing the graph.</p> Source code in <code>discrete_modulus/demo.py</code> <pre><code>def house_graph() -&gt; tuple[nx.Graph, PosDict]:\n    \"\"\"\n    Generates the house graph.\n\n    Returns\n    -------\n    G : networkx graph\n        The house graph.\n\n    pos : dict\n        Position dictionary for drawing the graph.\n    \"\"\"\n\n    G = nx.cycle_graph(5)\n    G.add_edge(0, 2)\n    pos = {0: (0, 1), 1: (0.5, 1.87), 2: (1, 1), 3: (1, 0), 4: (0, 0)}\n\n    return G, pos\n</code></pre>"},{"location":"reference/demo/#discrete_modulus.demo.nested_graph","title":"nested_graph","text":"<pre><code>nested_graph(n: int = 10) -&gt; tuple[nx.Graph, PosDict]\n</code></pre> <p>Generates a graph with three concentric \"layers\": a complete graph on <code>n</code> nodes, surrounded by two rings of <code>n</code> nodes each, with radial connections between consecutive layers.</p> <p>Parameters:</p> Name Type Description Default <code>n</code> <code>int</code> <p>The number of nodes in each layer (<code>3*n</code> nodes total).</p> <code>10</code> <p>Returns:</p> Name Type Description <code>G</code> <code>networkx graph</code> <p>The nested graph.</p> <code>pos</code> <code>dict</code> <p>Position dictionary for drawing the graph, placing the three layers on concentric circles.</p> Source code in <code>discrete_modulus/demo.py</code> <pre><code>def nested_graph(n: int = 10) -&gt; tuple[nx.Graph, PosDict]:\n    \"\"\"\n    Generates a graph with three concentric \"layers\": a complete graph on\n    `n` nodes, surrounded by two rings of `n` nodes each, with radial\n    connections between consecutive layers.\n\n    Parameters\n    ----------\n    n : int\n        The number of nodes in each layer (`3*n` nodes total).\n\n    Returns\n    -------\n    G : networkx graph\n        The nested graph.\n\n    pos : dict\n        Position dictionary for drawing the graph, placing the three\n        layers on concentric circles.\n    \"\"\"\n\n    G: nx.Graph = nx.Graph()\n\n    # complete graph on the innermost layer\n    for i in range(n):\n        for j in range(i + 1, n):\n            G.add_edge(i, j)\n\n    # connect each innermost node to three consecutive nodes in the\n    # middle layer, and form a cycle within the middle layer\n    for i in range(n):\n        for b in (-1, 0, 1):\n            j = (i + b) % n + n\n            G.add_edge(i, j)\n        j = (i + 1) % n + n\n        G.add_edge(i + n, j)\n\n    # radial connections to the outer layer, and a cycle within it\n    for i in range(n):\n        G.add_edge(i + n, i + 2 * n)\n        j = (i + 1) % n + 2 * n\n        G.add_edge(i + 2 * n, j)\n\n    # position the nodes in concentric circles\n    th = np.linspace(0, 2 * np.pi, n, endpoint=False)\n    pos: PosDict = {i: (float(np.cos(th[i])), float(np.sin(th[i]))) for i in range(n)}\n    pos.update({n + i: (2 * float(np.cos(th[i])), 2 * float(np.sin(th[i]))) for i in range(n)})\n    pos.update({2 * n + i: (3 * float(np.cos(th[i])), 3 * float(np.sin(th[i]))) for i in range(n)})\n\n    return G, pos\n</code></pre>"},{"location":"reference/demo/#discrete_modulus.demo.slashed_house_graph","title":"slashed_house_graph","text":"<pre><code>slashed_house_graph() -&gt; tuple[nx.Graph, PosDict]\n</code></pre> <p>Generates a house graph with a diagonal slash through it.</p> <p>Returns:</p> Name Type Description <code>G</code> <code>networkx graph</code> <p>The house graph.</p> <code>pos</code> <code>dict</code> <p>Position dictionary for drawing the graph.</p> Source code in <code>discrete_modulus/demo.py</code> <pre><code>def slashed_house_graph() -&gt; tuple[nx.Graph, PosDict]:\n    \"\"\"\n    Generates a house graph with a diagonal slash through it.\n\n    Returns\n    -------\n    G : networkx graph\n        The house graph.\n\n    pos : dict\n        Position dictionary for drawing the graph.\n    \"\"\"\n\n    G, pos = house_graph()\n    G.add_edge(0, 3)\n\n    return G, pos\n</code></pre>"},{"location":"reference/demo/#discrete_modulus.demo.spanning_trees","title":"spanning_trees","text":"<pre><code>spanning_trees(G: Graph) -&gt; Iterator[tuple[Edge, ...]]\n</code></pre> <p>Generates all spanning trees of a graph.</p> <p>Parameters:</p> Name Type Description Default <code>G</code> <code>networkx graph</code> required <p>Returns:</p> Type Description <code>generator</code> <p>Each item returned is a list of edges for a spanning tree. The generator will produce every tree before terminating.</p> Notes <p>The algorithm looks at ALL combinations of |V|-1 edges in G and determines which are trees.  This will be very very slow on large graphs, so use with care.</p> Source code in <code>discrete_modulus/demo.py</code> <pre><code>def spanning_trees(G: nx.Graph) -&gt; Iterator[tuple[Edge, ...]]:\n    \"\"\"\n    Generates all spanning trees of a graph.\n\n    Parameters\n    ----------\n    G : networkx graph\n\n    Returns\n    -------\n    generator\n        Each item returned is a list of edges for a spanning tree.\n        The generator will produce every tree before terminating.\n\n    Notes\n    -----\n    The algorithm looks at ALL combinations of |V|-1 edges in G and\n    determines which are trees.  This will be very very slow on large\n    graphs, so use with care.\n    \"\"\"\n\n    n = len(G.nodes)\n    for T in combinations(G.edges, n - 1):\n        H: nx.Graph = nx.Graph(T)\n        if nx.is_tree(H):\n            yield T\n</code></pre>"},{"location":"reference/protocols/","title":"Protocols","text":""},{"location":"reference/protocols/#discrete_modulus.protocols","title":"discrete_modulus.protocols","text":"<p>Shared types and structural interfaces (\"protocols\") used across the <code>discrete_modulus</code> package.</p> <p>These formalize the two informal callback conventions the rest of the package is built around:</p> <ul> <li> <p>A \"shortest object finder\" (see <code>ShortestObjectFinder</code>), used by   <code>algorithms.modulus</code> to grow the constraint set, and implemented by   <code>families.networkx_families.ShortestConnectingPath</code>,   <code>families.networkx_families.MinimumSpanningTree</code>,   <code>families.family_operators.UnionShortest</code>, and   <code>families.family_operators.SumShortest</code>.</p> </li> <li> <p>A \"subproblem solver\" (see <code>SubproblemSolver</code>), used by   <code>algorithms.modulus</code> to re-optimize as constraints are added, and   implemented by <code>algorithms.matrix_modulus</code>.</p> </li> </ul> <p>Anyone writing a new family or solver only needs to match these <code>__call__</code> signatures; there is no base class to inherit from.</p>"},{"location":"reference/protocols/#discrete_modulus.protocols.ExactArray","title":"ExactArray  <code>module-attribute</code>","text":"<pre><code>ExactArray = NDArray[np.object_]\n</code></pre> <p>A numpy object-dtype array, for exact-arithmetic alternatives to <code>FloatArray</code>.</p> <p>Not tied to any specific element type -- <code>dtype=object</code> erases it anyway, so this covers an array of <code>fractions.Fraction</code> (the element type used throughout this package, e.g. <code>min_norm_point</code>), but equally an array of <code>mpmath.mpf</code>, a custom interval type, or anything else whose <code>+</code>/<code>*</code>/<code>&lt;</code> etc. numpy's elementwise dispatch can call. Anywhere a <code>FloatArray</code> is accepted, an <code>ExactArray</code> may be used instead to get exact results end to end (e.g. through <code>families.networkx_families</code>'s MST/shortest-path lookups, which only rely on comparison and addition of edge weights).</p>"},{"location":"reference/protocols/#discrete_modulus.protocols.ModulusResult","title":"ModulusResult","text":"<p>               Bases: <code>NamedTuple</code></p> <p>The result of running <code>algorithms.modulus</code>.</p> <p>Attributes:</p> Name Type Description <code>mod</code> <code>float</code> <p>Approximation to modulus.</p> <code>cons</code> <code>list</code> <p>List of constraints added during the iteration. The format of the elements of this list is determined by the <code>cons</code> field returned by the <code>find_shortest</code> callable.</p> <code>rho</code> <code>numpy array</code> <p>Approximation to an optimal density.</p> <code>lam</code> <code>numpy array</code> <p>Approximation to the dual variables for the constraints listed in <code>cons</code>.</p>"},{"location":"reference/protocols/#discrete_modulus.protocols.ShortestObjectFinder","title":"ShortestObjectFinder","text":"<p>               Bases: <code>Protocol</code></p> <p>Protocol for callables that find a \"most violated constraint\" for a given family, given the current density <code>rho</code>.</p>"},{"location":"reference/protocols/#discrete_modulus.protocols.ShortestObjectFinder.__call__","title":"__call__","text":"<pre><code>__call__(\n    rho: FloatArray | ExactArray, tol: float\n) -&gt; ShortestResult\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>rho</code> <code>numpy array</code> <p>The current density. An <code>ExactArray</code> may be passed instead of a <code>FloatArray</code> to get an exact result, if the implementation supports it (see <code>ExactArray</code>).</p> required <code>tol</code> <code>float</code> <p>The tolerance; may be ignored by implementations that don't need it (see <code>ShortestResult</code>'s <code>n</code> field for the \"already admissible\" case this exists to support).</p> required <p>Returns:</p> Type Description <code>ShortestResult</code> Source code in <code>discrete_modulus/protocols.py</code> <pre><code>def __call__(self, rho: FloatArray | ExactArray, tol: float) -&gt; ShortestResult:\n    \"\"\"\n    Parameters\n    ----------\n    rho : numpy array\n        The current density. An `ExactArray` may be passed instead of\n        a `FloatArray` to get an exact result, if the implementation\n        supports it (see `ExactArray`).\n\n    tol : float\n        The tolerance; may be ignored by implementations that don't\n        need it (see `ShortestResult`'s `n` field for the \"already\n        admissible\" case this exists to support).\n\n    Returns\n    -------\n    ShortestResult\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/protocols/#discrete_modulus.protocols.ShortestResult","title":"ShortestResult","text":"<p>               Bases: <code>NamedTuple</code></p> <p>The result of a <code>ShortestObjectFinder</code> call.</p> <p>Attributes:</p> Name Type Description <code>cons</code> <code>object</code> <p>Any representation desired for describing the object found (e.g. a path, a set of edges). Purely informational; may be None.</p> <code>n</code> <code>numpy array or None</code> <p>The row vector representing the corresponding constraint, to be added to the usage matrix. None if every constraint is already satisfied to within tolerance.</p>"},{"location":"reference/protocols/#discrete_modulus.protocols.SubproblemResult","title":"SubproblemResult","text":"<p>               Bases: <code>NamedTuple</code></p> <p>The result of a <code>SubproblemSolver</code> call.</p> <p>Attributes:</p> Name Type Description <code>mod</code> <code>float</code> <p>Approximation to the modulus of the subproblem.</p> <code>rho</code> <code>numpy array</code> <p>Approximation to an optimal density.</p> <code>lam</code> <code>numpy array</code> <p>Approximation to the dual variables for the subproblem's constraints.</p>"},{"location":"reference/protocols/#discrete_modulus.protocols.SubproblemSolver","title":"SubproblemSolver","text":"<p>               Bases: <code>Protocol</code></p> <p>Protocol for callables that solve a modulus subproblem given a usage matrix <code>N</code>.</p>"},{"location":"reference/protocols/#discrete_modulus.protocols.SubproblemSolver.__call__","title":"__call__","text":"<pre><code>__call__(\n    N: FloatArray | spmatrix,\n    p: float,\n    sigma: FloatArray | None = None,\n) -&gt; SubproblemResult\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>N</code> <code>numpy array or scipy sparse matrix</code> <p>The usage matrix for the (sub)family being solved.</p> required <code>p</code> <code>float or inf</code> <p>The modulus energy exponent.</p> required <code>sigma</code> <code>numpy array</code> <p>The weights sigma. If None, all weights are treated as 1.</p> <code>None</code> <p>Returns:</p> Type Description <code>SubproblemResult</code> Source code in <code>discrete_modulus/protocols.py</code> <pre><code>def __call__(\n    self,\n    N: FloatArray | sp.spmatrix,\n    p: float,\n    sigma: FloatArray | None = None,\n) -&gt; SubproblemResult:\n    \"\"\"\n    Parameters\n    ----------\n    N : numpy array or scipy sparse matrix\n        The usage matrix for the (sub)family being solved.\n\n    p : float or np.inf\n        The modulus energy exponent.\n\n    sigma : numpy array, optional\n        The weights sigma. If None, all weights are treated as 1.\n\n    Returns\n    -------\n    SubproblemResult\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/families/family_operators/","title":"Family operators","text":""},{"location":"reference/families/family_operators/#discrete_modulus.families.family_operators","title":"discrete_modulus.families.family_operators","text":"<p>Operator functors on families.</p>"},{"location":"reference/families/family_operators/#discrete_modulus.families.family_operators.SumShortest","title":"SumShortest","text":"<pre><code>SumShortest(F: Sequence[ShortestObjectFinder])\n</code></pre> <p>Shortest object operator for a summation of families.</p> <p>Given a collection of families, finds the \"sum\" object obtained by combining the shortest object from each family -- i.e. implements <code>ShortestObjectFinder</code> for Gamma_1 + Gamma_2 + ... + Gamma_k.</p> <p>Parameters:</p> Name Type Description Default <code>F</code> <code>sequence of ShortestObjectFinder</code> <p>The finders for the families being summed.</p> required Source code in <code>discrete_modulus/families/family_operators.py</code> <pre><code>def __init__(self, F: Sequence[ShortestObjectFinder]) -&gt; None:\n    \"\"\"\n    Parameters\n    ----------\n    F : sequence of ShortestObjectFinder\n        The finders for the families being summed.\n    \"\"\"\n    self.F = F\n</code></pre>"},{"location":"reference/families/family_operators/#discrete_modulus.families.family_operators.SumShortest.__call__","title":"__call__","text":"<pre><code>__call__(\n    rho: FloatArray | ExactArray, tol: float\n) -&gt; ShortestResult\n</code></pre> <p>Combines the shortest object from each family in the sum.</p> <p>Parameters:</p> Name Type Description Default <code>rho</code> <code>numpy array</code> <p>The current density.</p> required <code>tol</code> <code>float</code> <p>Passed through to each finder in <code>F</code>; otherwise unused here.</p> required <p>Returns:</p> Type Description <code>ShortestResult</code> <p><code>cons</code> is the list of each family's individual result (in the order of <code>F</code>); <code>n</code> is the elementwise sum of their usage vectors, with the same dtype as <code>rho</code>.</p> Source code in <code>discrete_modulus/families/family_operators.py</code> <pre><code>def __call__(self, rho: FloatArray | ExactArray, tol: float) -&gt; ShortestResult:\n    \"\"\"\n    Combines the shortest object from each family in the sum.\n\n    Parameters\n    ----------\n    rho : numpy array\n        The current density.\n\n    tol : float\n        Passed through to each finder in `F`; otherwise unused here.\n\n    Returns\n    -------\n    ShortestResult\n        `cons` is the list of each family's individual result (in\n        the order of `F`); `n` is the elementwise sum of their usage\n        vectors, with the same dtype as `rho`.\n    \"\"\"\n\n    # mypy can't infer, from a union-typed rho, that dtype=rho.dtype\n    # produces a same-union-member array.\n    n: FloatArray | ExactArray = np.zeros(rho.shape, dtype=rho.dtype)  # type: ignore[assignment]\n    cons = []\n    for f in self.F:\n        result = f(rho, tol)\n        assert result.n is not None\n        cons.append(result.cons)\n        n = n + result.n\n\n    return ShortestResult(cons, n)\n</code></pre>"},{"location":"reference/families/family_operators/#discrete_modulus.families.family_operators.UnionShortest","title":"UnionShortest","text":"<pre><code>UnionShortest(F: Sequence[ShortestObjectFinder])\n</code></pre> <p>Shortest object operator for a union of families.</p> <p>Given a collection of families (each represented by a <code>ShortestObjectFinder</code>), finds the single shortest object across all of them -- i.e. implements <code>ShortestObjectFinder</code> for Gamma_1 U Gamma_2 U ... U Gamma_k.</p> <p>Parameters:</p> Name Type Description Default <code>F</code> <code>sequence of ShortestObjectFinder</code> <p>The finders for the families being unioned.</p> required Source code in <code>discrete_modulus/families/family_operators.py</code> <pre><code>def __init__(self, F: Sequence[ShortestObjectFinder]) -&gt; None:\n    \"\"\"\n    Parameters\n    ----------\n    F : sequence of ShortestObjectFinder\n        The finders for the families being unioned.\n    \"\"\"\n    self.F = F\n</code></pre>"},{"location":"reference/families/family_operators/#discrete_modulus.families.family_operators.UnionShortest.__call__","title":"__call__","text":"<pre><code>__call__(\n    rho: FloatArray | ExactArray, tol: float\n) -&gt; ShortestResult\n</code></pre> <p>Finds the shortest object across all families in the union.</p> <p>Parameters:</p> Name Type Description Default <code>rho</code> <code>numpy array</code> <p>The current density.</p> required <code>tol</code> <code>float</code> <p>Passed through to each finder in <code>F</code>; otherwise unused here.</p> required <p>Returns:</p> Type Description <code>ShortestResult</code> <p>The result from whichever finder in <code>F</code> produced the object of smallest rho-length.</p> Source code in <code>discrete_modulus/families/family_operators.py</code> <pre><code>def __call__(self, rho: FloatArray | ExactArray, tol: float) -&gt; ShortestResult:\n    \"\"\"\n    Finds the shortest object across all families in the union.\n\n    Parameters\n    ----------\n    rho : numpy array\n        The current density.\n\n    tol : float\n        Passed through to each finder in `F`; otherwise unused here.\n\n    Returns\n    -------\n    ShortestResult\n        The result from whichever finder in `F` produced the object\n        of smallest rho-length.\n    \"\"\"\n\n    results = [f(rho, tol) for f in self.F]\n    lengths = []\n    for result in results:\n        assert result.n is not None\n        lengths.append(rho.dot(result.n))\n    ind = np.argmin(lengths)\n    return results[ind]\n</code></pre>"},{"location":"reference/families/networkx_families/","title":"NetworkX families","text":""},{"location":"reference/families/networkx_families/#discrete_modulus.families.networkx_families","title":"discrete_modulus.families.networkx_families","text":"<p>Functor classes for various families of objects implemented in NetworkX.</p>"},{"location":"reference/families/networkx_families/#discrete_modulus.families.networkx_families.MinimumSpanningTree","title":"MinimumSpanningTree","text":"<pre><code>MinimumSpanningTree(G: Graph)\n</code></pre> <p>Functor class for finding the minimum rho-length spanning tree.</p> <p>Implements <code>ShortestObjectFinder</code> for the family of spanning trees of <code>G</code>. <code>G</code> may be a <code>nx.Graph</code> or a <code>nx.MultiGraph</code> -- a shrunk multigraph produced by repeatedly contracting a graph's vertices (as the C++ solver's rounds do, and as <code>core_deflation</code> does when contracting a core to a point) generally has parallel edges, so both are supported: with a <code>MultiGraph</code>, <code>cons</code> disambiguates parallel edges as <code>(u, v, key)</code> triples instead of plain <code>(u, v)</code> pairs.</p> <p>Parameters:</p> Name Type Description Default <code>G</code> <code>networkx graph or multigraph</code> <p>The graph whose spanning trees make up the family.</p> required Notes <p>This mutates <code>G</code> by adding an <code>'enum'</code> edge attribute (an index into <code>rho</code>/the usage matrix), and, on each call, a <code>'rho'</code> edge attribute holding the most recently assigned density. Iterating with <code>data=True</code> and mutating the yielded attribute dict in place (rather than indexing <code>G[u][v]</code>, which for a <code>MultiGraph</code> is a dict-of-parallel-edges, not a single edge's attributes) keeps this correct for both graph types.</p> Source code in <code>discrete_modulus/families/networkx_families.py</code> <pre><code>def __init__(self, G: nx.Graph) -&gt; None:\n    \"\"\"\n    Parameters\n    ----------\n    G : networkx graph or multigraph\n        The graph whose spanning trees make up the family.\n\n    Notes\n    -----\n    This mutates `G` by adding an `'enum'` edge attribute (an index\n    into `rho`/the usage matrix), and, on each call, a `'rho'` edge\n    attribute holding the most recently assigned density. Iterating\n    with `data=True` and mutating the yielded attribute dict\n    in place (rather than indexing `G[u][v]`, which for a\n    `MultiGraph` is a dict-of-parallel-edges, not a single edge's\n    attributes) keeps this correct for both graph types.\n    \"\"\"\n\n    # remember the graph\n    self.G = G\n    self.is_multigraph = G.is_multigraph()\n\n    # enumerate the edges so we can keep track of them when\n    # processing a spanning tree\n    for i, (_u, _v, data) in enumerate(G.edges(data=True)):\n        data[\"enum\"] = i\n</code></pre>"},{"location":"reference/families/networkx_families/#discrete_modulus.families.networkx_families.MinimumSpanningTree.__call__","title":"__call__","text":"<pre><code>__call__(\n    rho: FloatArray | ExactArray, tol: float\n) -&gt; ShortestResult\n</code></pre> <p>Finds a minimum rho-length spanning tree of <code>G</code>.</p> <p>Parameters:</p> Name Type Description Default <code>rho</code> <code>numpy array</code> <p>The current density, one entry per edge of <code>G</code> (in the order <code>G.edges()</code> iterates). May be an <code>ExactArray</code> for an exact result (see <code>ExactArray</code>).</p> required <code>tol</code> <code>float</code> <p>Unused; accepted to satisfy the <code>ShortestObjectFinder</code> interface.</p> required <p>Returns:</p> Type Description <code>ShortestResult</code> <p><code>cons</code> is the list of edges in the minimum spanning tree -- <code>(u, v)</code> pairs for a plain <code>Graph</code>, <code>(u, v, key)</code> triples for a <code>MultiGraph</code>. <code>n</code> is its usage vector, with the same dtype as <code>rho</code>.</p> Source code in <code>discrete_modulus/families/networkx_families.py</code> <pre><code>def __call__(self, rho: FloatArray | ExactArray, tol: float) -&gt; ShortestResult:\n    \"\"\"\n    Finds a minimum rho-length spanning tree of `G`.\n\n    Parameters\n    ----------\n    rho : numpy array\n        The current density, one entry per edge of `G` (in the order\n        `G.edges()` iterates). May be an `ExactArray` for an exact\n        result (see `ExactArray`).\n\n    tol : float\n        Unused; accepted to satisfy the `ShortestObjectFinder`\n        interface.\n\n    Returns\n    -------\n    ShortestResult\n        `cons` is the list of edges in the minimum spanning tree --\n        `(u, v)` pairs for a plain `Graph`, `(u, v, key)` triples for\n        a `MultiGraph`. `n` is its usage vector, with the same dtype\n        as `rho`.\n    \"\"\"\n\n    # assign rho to the graph edges\n    for i, (_u, _v, data) in enumerate(self.G.edges(data=True)):\n        data[\"rho\"] = rho[i]\n\n    # form the row vector. mypy can't infer, from a union-typed rho,\n    # that dtype=rho.dtype produces a same-union-member array.\n    n: FloatArray | ExactArray = np.zeros(rho.shape, dtype=rho.dtype)  # type: ignore[assignment]\n\n    if self.is_multigraph:\n        # keys=True disambiguates which parallel edge the MST used\n        # -- needed both to look up its 'enum' index below and so\n        # `cons` identifies a genuine spanning tree, not just a\n        # multiset of vertex pairs.\n        T = list(nx.minimum_spanning_edges(self.G, weight=\"rho\", keys=True, data=False))\n        for u, v, k in T:\n            n[self.G[u][v][k][\"enum\"]] = 1\n    else:\n        T = list(nx.minimum_spanning_edges(self.G, weight=\"rho\", data=False))\n        for u, v in T:\n            n[self.G[u][v][\"enum\"]] = 1\n\n    return ShortestResult(T, n)\n</code></pre>"},{"location":"reference/families/networkx_families/#discrete_modulus.families.networkx_families.ShortestConnectingPath","title":"ShortestConnectingPath","text":"<pre><code>ShortestConnectingPath(G: Graph, S: Iterable, T: Iterable)\n</code></pre> <p>Functor class for finding the shortest rho-length path between two sets of nodes.</p> <p>Implements <code>ShortestObjectFinder</code> for the family of paths connecting any node in <code>S</code> to any node in <code>T</code>.</p> <p>Parameters:</p> Name Type Description Default <code>G</code> <code>networkx graph</code> <p>The graph the family of paths lives in.</p> required <code>S</code> <code>iterable of nodes</code> <p>The set of allowed path start nodes.</p> required <code>T</code> <code>iterable of nodes</code> <p>The set of allowed path end nodes.</p> required Notes <p>This mutates <code>G</code> by adding an <code>'enum'</code> edge attribute (an index into <code>rho</code>/the usage matrix). A copy of <code>G</code>, with dummy source and target nodes attached, is kept internally for the shortest-path search; <code>G</code> itself is otherwise left untouched.</p> Source code in <code>discrete_modulus/families/networkx_families.py</code> <pre><code>def __init__(self, G: nx.Graph, S: Iterable, T: Iterable) -&gt; None:\n    \"\"\"\n    Parameters\n    ----------\n    G : networkx graph\n        The graph the family of paths lives in.\n\n    S : iterable of nodes\n        The set of allowed path start nodes.\n\n    T : iterable of nodes\n        The set of allowed path end nodes.\n\n    Notes\n    -----\n    This mutates `G` by adding an `'enum'` edge attribute (an index\n    into `rho`/the usage matrix). A copy of `G`, with dummy source\n    and target nodes attached, is kept internally for the\n    shortest-path search; `G` itself is otherwise left untouched.\n    \"\"\"\n\n    # remember the graph, source and target sets\n    self.G = G\n    self.S = S\n    self.T = T\n\n    # enumerate the edges so we can keep track of them\n    # when processing a path\n    for i, (u, v) in enumerate(G.edges()):\n        G[u][v][\"enum\"] = i\n\n    # make a copy of G to work on\n    self.H = G.copy()\n\n    # add dummy source and target nodes\n    self.H.add_node(self.src)\n    self.H.add_node(self.tgt)\n\n    # link the dummy nodes\n    for v in S:\n        self.H.add_edge(self.src, v, rho=0)\n    for v in T:\n        self.H.add_edge(v, self.tgt, rho=0)\n</code></pre>"},{"location":"reference/families/networkx_families/#discrete_modulus.families.networkx_families.ShortestConnectingPath.__call__","title":"__call__","text":"<pre><code>__call__(\n    rho: FloatArray | ExactArray, tol: float\n) -&gt; ShortestResult\n</code></pre> <p>Finds the shortest rho-length path from <code>S</code> to <code>T</code>.</p> <p>Parameters:</p> Name Type Description Default <code>rho</code> <code>numpy array</code> <p>The current density, one entry per edge of <code>G</code> (in the order <code>G.edges()</code> iterates). May be an <code>ExactArray</code> for an exact result (see <code>ExactArray</code>).</p> required <code>tol</code> <code>float</code> <p>Unused; accepted to satisfy the <code>ShortestObjectFinder</code> interface.</p> required <p>Returns:</p> Type Description <code>ShortestResult</code> <p><code>cons</code> is the path found, as a list of nodes from <code>S</code> to <code>T</code> (the dummy source/target nodes are already trimmed off). <code>n</code> is its usage vector, with the same dtype as <code>rho</code>.</p> Source code in <code>discrete_modulus/families/networkx_families.py</code> <pre><code>def __call__(self, rho: FloatArray | ExactArray, tol: float) -&gt; ShortestResult:\n    \"\"\"\n    Finds the shortest rho-length path from `S` to `T`.\n\n    Parameters\n    ----------\n    rho : numpy array\n        The current density, one entry per edge of `G` (in the order\n        `G.edges()` iterates). May be an `ExactArray` for an exact\n        result (see `ExactArray`).\n\n    tol : float\n        Unused; accepted to satisfy the `ShortestObjectFinder`\n        interface.\n\n    Returns\n    -------\n    ShortestResult\n        `cons` is the path found, as a list of nodes from `S` to `T`\n        (the dummy source/target nodes are already trimmed off).\n        `n` is its usage vector, with the same dtype as `rho`.\n    \"\"\"\n\n    # assign rho to the graph edges\n    for i, (u, v) in enumerate(self.G.edges()):\n        self.H[u][v][\"rho\"] = rho[i]\n\n    # find the shortest path\n    p = nx.shortest_path(self.H, self.src, self.tgt, weight=\"rho\")\n\n    # the actual path omits the source and target dummy nodes\n    p = p[1:-1]\n\n    # form the row vector. mypy can't infer, from a union-typed rho,\n    # that dtype=rho.dtype produces a same-union-member array.\n    n: FloatArray | ExactArray = np.zeros(rho.shape, dtype=rho.dtype)  # type: ignore[assignment]\n    for i in range(len(p) - 1):\n        n[self.G[p[i]][p[i + 1]][\"enum\"]] = 1\n\n    return ShortestResult(p, n)\n</code></pre>"}]}